const HtmlWebpackPlugin = require("html-webpack-plugin");
const { resoveDev } = require("../build/unit");

const HTMLTEMP = [
{{#each list}}
new HtmlWebpackPlugin({
filename: "{{name}}.html",
template: resoveDev("/{{name}}/{{name}}.html"),
title: "{{name}}",
chunks: ["{{name}}", "common"],
minify: {
removeComments: true,
collapseWhitespace: false,
removeAttributeQuotes: false,
//压缩html中的js
minifyJS: false,
//压缩html中的css
minifyCSS: false,
},
}),
{{/each}}
];

module.exports = HTMLTEMP;